export const MESSAGE_STATUS_OPTIONS = ['all', 'new', 'read', 'replied', 'archived'] as const;

export const PRIORITY_VALUES = ['all', 'high', 'medium', 'low'] as const;

export const SORT_OPTIONS = ['newest', 'oldest', 'name', 'priority'] as const;

export const ITEMS_PER_PAGE_OPTIONS = [10, 25, 50, 100] as const;
